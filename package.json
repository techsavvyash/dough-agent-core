{
  "name": "dough",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "bun run --filter @dough/server start",
    "tui": "bun run --filter @dough/tui start",
    "dev": "tmux kill-session -t dough 2>/dev/null; lsof -ti :4200 | xargs kill -9 2>/dev/null; tmux new-session -d -s dough 'bun run packages/server/src/index.ts 2>&1; read' && sleep 2 && tmux new-window -t dough 'cd $PWD && bun run packages/tui/src/index.tsx 2>&1' && tmux select-window -t dough:1 && tmux attach -t dough",
    "build": "bunx tsc --noEmit",
    "test": "bun test packages/",
    "test:unit": "bun test packages/core packages/server packages/threads packages/protocol packages/sdk",
    "test:ui": "bun test packages/tui",
    "typecheck": "bunx tsc --noEmit",
    "commit": "cz",
    "prepare": "husky",
    "dough": "bun run packages/cli/src/index.ts"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/bun": "latest",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7",
    "typescript": "^5"
  }
}
